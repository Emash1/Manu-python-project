mytuple=("sidney","smith","allan")

"""
1.elements in a tuple are indexed
2.tuples are usually in round brackets
3.tuples are allow duplicates
4.tuples can have elements of different data types
5.Tuples are immutable i.e they cannot be changed directly
"""
print(mytuple[1])

#to modify/add/remove tuples
"""
1.convert the tuple into a list
        mylist=list(thetuplename)  
2modify the value you wanted the same way we did in lists
example on modification:
        student[0]="ken"
example on deleting:
        student.remove(student[0])
example on adding:
        student.append("mercy")
3.convert it back to a tuple
        mytuple=tuple(listname)  
"""
